# Sample: repo_sub

Minimal product tree for third **repo-mode ACCEPT** proof (corpus growth).

Starts broken (`a + b` instead of `a - b`). Task brief: "Fix the broken subtract function".

```bash
specialized-harness run --repo samples/repo_sub --task "Fix the broken subtract function"
```

Harness decides ACCEPT only via ledger + pytest in the workspace.
