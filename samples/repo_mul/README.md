# Sample: repo_mul

Minimal product tree for second **repo-mode ACCEPT** proof (broader corpus).

Starts broken (`a / b`). Task brief: "Fix the broken multiply function".

```bash
specialized-harness run --repo samples/repo_mul --task "Fix the broken multiply function"
```

Harness decides ACCEPT only via ledger + pytest in the workspace.
