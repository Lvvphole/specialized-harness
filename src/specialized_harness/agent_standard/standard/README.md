# Packaged Agent Engineering Standard

Shipped inside the wheel so installed distributions can load candidate-construction guidance without a source checkout of `.agent-standard/`.

Authority boundary unchanged: harness ledger + CI declare ACCEPT/HANDOFF; this package only supplies rule text for `AgentProvider.propose`.
