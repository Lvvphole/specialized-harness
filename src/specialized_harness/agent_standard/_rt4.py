CHUNK = '            if not self._rule_applies(local, task_profile.id, language):\n                continue\n            matches.append(local)\n\n        if not matches:\n            return None\n\n        lines = [\n            "<engineering_standard_remediation>",\n            f"Deterministic check: {check_ref}",\n            "Use the raw checker output as the verification evidence. "\n            "The rules below only explain the applicable engineering constraint.",\n        ]\n        for rule in matches:\n            lines.append(f"- {rule.id} [{rule.level}] {rule.instruction}")\n        lines.append("</engineering_standard_remediation>")\n        return "\\n".join(lines)\n'
